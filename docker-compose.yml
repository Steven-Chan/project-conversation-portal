version: "3.8"
services:
  db:
    image: postgres:15.3
    ports:
      - 5432:5432
    volumes:
      - db_data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: 1234Qwer!

volumes:
  db_data:
